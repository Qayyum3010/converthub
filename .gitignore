# Dependencies
node_modules/

# Environment / secrets
.env
.env.local
.env.*.local

# Build output
dist/
build/
.next/
out/

# Logs
*.log
npm-debug.log*

# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Project docs (working/planning files, not shipped code)
/project-docs/
# Local export tooling (not shipped)
/PROJECT_DUMP.txt
/PROJECT_EXPORT.ps1
# Manual test payloads (local scratch, not shipped)
/convert-payload*.json

# Temp upload/processing storage (if used locally)
/tmp/
/uploads/

client/.env.local

# Large binary test fixtures (not needed in version control — see
# DECISIONS.md Task 5.10.6 for how to regenerate/redownload if needed)
test-fixtures/sample.iso
# Test scripts (one-off verification runs, not shipped)
/run-*.sh
/full-registry-test.sh
/cls

# Large test fixture library (not shipped — see DECISIONS.md for how to
# regenerate if needed)
/test-fixtures/